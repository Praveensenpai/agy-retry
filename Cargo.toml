[package]
name = "agy-retry"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "agy-retry"
path = "src/main.rs"

[dependencies]
libc = "0.2"
regex = "1"
serde = { version = "1.0.229", features = ["derive"] }
toml = "1.1.5"
